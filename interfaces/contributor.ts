export interface IContributor {
  base64: string;
  core: boolean;
  description: string;
  github: string;
  iconCount: number;
  id: string;
  name: string;
  sponsored: boolean;
  twitter?: string;
  website?: string;
}
